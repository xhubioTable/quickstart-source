In order to run this quickstart demo first install all dependencies of this project with
yarn install
and then run test data generation with:
node src/quickstart.js
The generated data will be available in the tdg folder.

